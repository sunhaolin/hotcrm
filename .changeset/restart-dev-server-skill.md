---
---

Agent tooling only — a `.claude/` skill that records how to restart the dev
server. Nothing in the app changes, so this ships nothing to a release-notes
reader (AGENTS.md § Verifying changes, empty-frontmatter exception).
