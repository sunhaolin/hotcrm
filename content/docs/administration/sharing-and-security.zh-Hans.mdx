---
title: 共享与安全
description: 谁能查看和编辑什么 —— 组织范围默认值、岗位、共享规则和字段级安全。
---

# 共享与安全

共享回答这个问题：*“这个用户能查看和编辑哪些记录？”* 四个层级叠加在一起给出答案：

1. **对象权限** —— 这个用户到底能不能碰这个对象？（参见[权限档案](/zh-Hans/docs/administration/profiles)。）
2. **组织范围默认值 (OWD)** —— 记录级的基线（“商机是私有的”）。
3. **共享规则** —— 各种开口（“EU 团队可以编辑 EU 客户”）。
4. **手工共享** —— 记录级的“你和我一起在跟这一条”。

外加用于遮罩敏感字段的 **字段级安全**。

> 第一层是闸门，不是过滤器。如果权限档案没有授予这个对象，任何 OWD、共享规则或 *查看所有* 设置都无法把记录找回来 —— 请求在触及共享之前就被拒绝了。

## 第 1 层 —— 组织范围默认值

OWD 是每个对象的 *最严格* 设置，其他一切都在此之上放宽。

| 对象 | OWD | 原因 |
| --- | --- | --- |
| 线索 | 私有 | 代表跟自己的线索 |
| 客户 | 私有 | 客户归属很重要 |
| 联系人 | 由父级控制（客户） | 联系人属于它的客户，并跟随客户的共享 —— 见下面的 *“由父级控制”在实践中* |
| 商机 | 私有 | 敏感的交易数据 |
| 商机行项 | 由父级控制（商机） | 产品行是交易的一部分 |
| 报价 | 私有 | 定价属于所有人 |
| 报价行项 | 由父级控制（报价） | 行项是报价的一部分 |
| 合同 | 私有 | 敏感的商务条款 |
| 工单 | 私有 | 专员的队列是自己的；升级件走共享 |
| 任务 | 私有 | 个人活动条目 |
| 活动 | 私有 | 个人活动条目，与任务同类 |
| 活动参与者 | 由父级控制（活动） | 参与者属于他们参加的那场会议，并跟随那场会议的共享 —— 见下面的 *“由父级控制”在实践中* |
| 预测 | 私有 | 快照属于其所有人；经理持有 *查看所有* |
| 产品 | 公共只读 | 所有人都看得到目录 |
| 营销活动 | 公共只读 | 营销创建，销售查看 |
| 营销活动成员 | 由父级控制（营销活动） | 成员关系是营销活动的一个属性，并跟随它的共享 —— 见下面的 *“由父级控制”在实践中* |
| 知识文章 | 公共只读 | 知识库面向全体内部用户；由专员撰写 |
| 文章反馈 | 由父级控制（知识文章） | 读者的"有用／没用"评价是知识文章的一个属性，并跟随它的共享 —— 见下面的 *“由父级控制”在实践中* |
| 售前项目 | 私有 | 投标属于跟进它的售前团队；销售经理与管理员持有 *查看所有* |
| 交付项目 | 私有 | 成本、毛利与人员安排是项目团队自己的事 |
| 成本计划 | 私有 | 一个计划版本是其成本管理员的工作记录；销售经理与管理员持有 *查看所有* |
| 人工服务成本行 | 由父级控制（成本计划） | 人工行是某一个计划版本的一部分，跟随计划的共享 |
| 第三方服务成本行 | 由父级控制（成本计划） | 第三方服务行是某一个计划版本的一部分，跟随计划的共享 |
| 软硬件采购成本行 | 由父级控制（成本计划） | 采购行是某一个计划版本的一部分，跟随计划的共享 |
| 项目费用成本行 | 由父级控制（成本计划） | 费用行是某一个计划版本的一部分，跟随计划的共享 |
| 月度分解行 | 由父级控制（成本计划） | 月度台账由计划的明细行分解而来，跟随计划 |
| 差旅标准 | 公共读写 | 每一条差旅费用行据此定价的主数据；由 PMO 维护 |
| 工时表 | 私有 | 工时表是填报人自己的月度记录；上面的交付项目可选，没有可供派生的父记录 |
| 差旅成本 | 由父级控制（交付项目） | 差旅费用是项目实际成本的一部分 |
| 费率卡 | 公共读写 | 所有人据此定价的主数据；由 PMO 维护 |
| 签约主体 | 公共读写 | 每个商机都要选的主数据；由财务维护 |
| 预算调整 | 由父级控制（交付项目） | 一次调整改变的是某一个项目的预算，跟随该项目 |
| 开票 | 由父级控制（交付项目） | 按项目开票，随项目可见 |
| 收款 | 由父级控制（交付项目） | 针对项目发票收到的款 |
| 采购合同 | 由父级控制（交付项目） | 分包与采购是项目成本 |
| 销售订单 | 由父级控制（交付项目） | 订单履行的是某一个项目的合同 |
| 出差申请 | 私有 | 出差是出差人自己的申请；审批人通过审批到达它 |
| 请假申请 | 私有 | 请假是申请人自己的记录；审批人通过审批到达它 |

**OWD 的取值**：私有 / 公共只读 / 公共读写 / 由父级控制。

### “由父级控制”在实践中

对于上面十七个由父级派生的对象，没有任何按用户配置的东西：访问权限是 *计算* 出来的。

- **读** —— 你看到的是那些父记录你能看到的行，而“能看到”指的是任何一条能让你直接打开该父记录的途径：你拥有它、某条共享规则授予了它，或者有人手工共享给了你。对随产品发布的 stack 实测（平台 17.0.0-rc.4）：一个只读得到一个客户的代表，只读到 **那一个** 客户的联系人；一个一张报价都读不到的代表，一条报价行项也读不到。那个仅通过区域规则到达该代表的客户正是关键 —— 通过共享持有的父记录，同样算作“你能看到的父记录”。
- **写** —— 新增、修改或移除一行需要对父记录有 **编辑** 权限，其解析方式与读取相同。父记录你无权编辑的行会被拒绝，并在报错中点名是哪个父记录挡下了它。

> **这一行为在平台 17.0.0-rc.4 发生了改变。** 在 17.0.0-rc.3 及更早版本上，计算出来的结果是组织范围而不是按父记录收窄：派生只从父对象自身的 *行级安全策略* 去解析可访问的父记录，而 HotCRM 几乎没有写这类策略，因此只要权限档案授予了该对象，“由父级控制”对象对该用户就是可读、也可写的。如果你运行的是更早的版本，你拿到的就是那个行为，报价与商机的行项定价在组织范围内可见。本仓的测试 `test/parent-derived-reach.test.ts` 在两个方向上都钉住了当前的可达范围（#549、#694；上游修复 objectstack-ai/objectstack#5386）。

由于访问是派生的，子记录本身的归属无关紧要 —— 报价生成流程创建的报价行，对报价所有人来说与自己手动录入的一样可见。

## 第 2 层 —— 岗位

**岗位（position）** 是用于分发访问权限的扁平群体。HotCRM 内置十二个：

```
executive
sales_director      sales_manager      sales_rep
service_director    service_manager    service_agent
marketing_director  marketing_manager  marketing_user
na_sales_team       eu_sales_team          （区域分组）
```

岗位 **在设计上是扁平的** —— 没有层级，**什么都不会上卷**。销售总监不会继承销售经理的访问权限；每个需要某条记录的层级都由它自己的共享规则点名。这就是下面若干规则成对出现的原因。

在 **设置 → 用户** 中分配岗位；一个用户可以持有多个。

## 第 3 层 —— 共享规则

共享规则在 OWD 基线之上 **放开** 记录访问。

### 两种形式

| 类型 | 共享什么 | 示例 |
| --- | --- | --- |
| **基于条件** | 匹配某个过滤条件的所有记录 | *“账单国家为 DE/FR/… 的客户”* → 共享给 EU 团队 |
| **基于所有人** | 某个群体拥有的所有记录 | *“SDR 拥有的记录”* → 共享给 AE 群体 |

基于条件的规则是被真正执行的那一种：匹配的记录会实体化为真实的共享授权。

### 内置共享规则

| 规则 | 对象 | 授予 | 给谁 |
| --- | --- | --- | --- |
| Account Team Sharing | 客户 | 编辑 | `sales_manager` —— 活跃的客户型客户 |
| North America Territory | 客户 | 编辑 | `na_sales_team` —— 销售区域为 `na` 的客户 |
| Europe Territory | 客户 | 编辑 | `eu_sales_team` —— 销售区域为 `emea` 的客户 |
| Opportunity Sales Team Sharing | 商机 | 读取 | `sales_director` —— 金额 ≥ $100,000 的进行中交易 |
| Large Open Deals — Executive | 商机 | 读取 | `executive` —— 同一批交易，上一层级 |
| Escalated Cases Sharing | 工单 | 编辑 | `service_manager` —— 未关闭的严重工单 |
| Escalated Cases — Service Director | 工单 | 读取 | `service_director` —— 同一批工单，上一层级 |
| Unassigned Cases — Triage | 工单 | 编辑 | `service_agent` —— 没有负责人的工单，直到它被 Resolved 或 Closed |
| Live Campaigns — Marketing Manager | 营销活动 | 编辑 | `marketing_manager` —— 筹划中 / 进行中的活动 |
| Live Campaigns — Marketing Director | 营销活动 | 编辑 | `marketing_director` —— 同一批活动 |

在 **设置 → 共享规则** 中创建你自己的规则。

> **条件只能过滤普通字段，不能过滤地址的某一部分。** 两条区域规则匹配的是客户上的 **账单国家** —— 一个只读的两位国家代码，由你在 **账单地址** 中填写的国家推导而来 —— 而不是地址本身。条件规则必须以数据库查询的形式运行，而查询无法深入结构化的地址或位置值内部。因此，针对 `账单地址 → 国家` 编写的规则会被**直接拒绝**，而不是被悄悄放宽成"所有客户"：它根本不会被安装，它指定的岗位也就什么都拿不到。界面上不会把这样的规则标记为失效，所以当某个团队反馈记录缺失时，请检查规则里引用的每个字段是否都是该对象上的普通字段。

### 一条为「被用掉」而设的授权：*Unassigned Cases — Triage*

大多数共享规则是常设的开口：今天匹配某条记录的规则明天照样匹配它，只要记录还符合条件，接收方就一直持有那份访问权。*Unassigned Cases — Triage* 是唯一一条**为被用掉而设计**的规则。它把**编辑**权授予每一位 `service_agent`，覆盖没有负责人、且既不是 *Resolved* 也不是 *Closed* 的工单——也就是工单列表里 **Unassigned — triage** 标签页背后的那条授权——它存在的意义，就是让某位客服把其中一张工单接走。而接走它，正是这份授权终结的方式：

1. 一张工单**没有负责人**地到达——比如没有人担任服务坐席时 Web 提交进来的那种。规则匹配上了它，于是每一位客服都能看到并编辑它。
2. 某位客服把它转到 **In Progress**、**Waiting on Customer** 或 **Waiting on Support**。应用在同一次保存里把这位客服标记为该工单的**负责人**。
3. 工单现在有负责人了，规则不再匹配它。它退出共享池、也退出那个标签页，而这位客服从此以负责人的身份、用最普通的方式持有它。

> **认领会写入负责人，却不需要任何人持有转移权限——这是设计，不是漏洞。** 客服专员只能转移任务（见下文*归属与转移*），而带着负责人字段的保存对客服一律被拒绝，无论填的是谁，填他自己也一样。所以认领不是一次「被批准的负责人编辑」，而是一个**被应用读作「我把这张接了」的状态变更**：它能写入的名字只有调用者自己，而且只能写到一张还没有负责人的工单上。「替别人认领」和「认领一张已经有主的工单」根本没有写法，这正是这个动作不需要任何新权限的原因。它也不是客服专属的：任何人把一张无主的未结工单转入那三种状态，都以同样的方式认领了它，管理员和服务经理也不例外。自动化则不会——种子写入或系统写入让工单保持无主，这正是首次安装时那些工单还能留在标签页里被看见的原因。

### 一条规则放开的是一个对象，而不是它下面的记录

共享规则是 **按对象** 编写的。放开 `Account` 只放开客户本身 —— 挂在这个客户下的记录仍然各自守着自己的基线，其中只有联系人是从客户派生的：

| 已共享客户上的相关列表 | 接收者实际看到的 |
| --- | --- |
| 联系人 | 这个客户的联系人 —— Contact 是“由父级控制”，而共享过来的客户就算作他们能看到的父记录 |
| 商机 | 只有自己的交易；若持有 `sales_director` 或 `executive`，另加金额 ≥ $100,000 的未结交易 |
| 报价 | 只有自己的 |
| 合同 | 只有自己的 |
| 工单 | 只有自己的；若持有 `service_manager` 或 `service_director`，另加未关闭的严重工单 |
| 任务 | 只有自己的 |
| 活动 | 只有自己的 |
| 售前项目 | 只有自己的 —— 售前项目是私有对象，没有规则放开它 |
| 交付项目 | 只有自己的 —— 交付项目是私有对象，没有规则放开它 |
| 开票 | 跟随它所属的交付项目，而不是客户 —— 开票由父级（交付项目）控制 |
| 收款 | 跟随它所属的交付项目，而不是客户 —— 收款由父级（交付项目）控制 |
| 销售订单 | 跟随它所属的交付项目，而不是客户 —— 销售订单由父级（交付项目）控制 |

也就是说，区域规则交出的是客户记录；下面的交易、报价、合同、工单、任务和会议历史仍归各自的所有人，用户明明能完整读取这个客户，这些相关列表却可能是空的或残缺的。联系人是例外，而且是符合本意的那种例外：它跟随客户，所以区域共享过来的客户，其联系人列表是完整的。

让子对象跟随客户是一次有意的放开，而不是一个配置细节。在子对象上按与客户规则匹配的条件另写一条共享规则，会对 **所有** 持有该对象的用户放开可见性，而不只是区域规则的接收者。把子对象的 OWD 改成“由父级控制”则是更精准的那把刀：自平台 17.0.0-rc.4 起，它恰好把子记录授予那些能够到达其父记录的人（见上面的 *“由父级控制”在实践中*）。对报价和合同，HotCRM 今天两者都不做；是否应该做是一项待定决策（#549）。

## 第 4 层 —— 手工共享

**手工共享** 在上述一切之上，把某一条记录的 **读取** 或 **读写** 权限授予某个用户或用户组。共享按记录存储。自平台 17.0.0-rc.4 起，手工共享同样会带上这条记录下由父级派生的子记录：共享一个客户就交出了它的联系人，因为派生会像解析其它途径一样把共享授权折算进去（见 *“由父级控制”在实践中*）。

HotCRM **不提供客户团队对象** —— 客户上没有团队名单，也没有“团队”页签。上表中那条名为 *Account Team Sharing* 的规则就是一条普通的条件规则：它把活跃客户类账户的编辑权授予每一位 `sales_manager`，而不是按记录组建的小组。以小组方式作战的组织，可以把小组建模为一个岗位并为其编写共享规则，或者在客户上使用手工共享。

## 这些层级如何组合

当以下条件 **全部** 成立时用户才能看到一条记录：其权限档案授予了该 **对象的读取权限**，并且 **以下任意一条** 为真：

1. 他们 **拥有** 这条记录。
2. 其权限档案授予该对象的 **查看所有**。
3. **OWD** 为公共只读或更宽。
4. 该对象是 **“由父级控制”**，且 **它的父记录** 通过上述任意一条途径对他们可见（见 *“由父级控制”在实践中*）。
5. 某条 **共享规则** 匹配该记录且命中他们的某个岗位。
6. 该记录被 **手工共享** 给了他们。

若都不成立，这条记录只是从列表、搜索和报表中 *消失* —— 而不是给出一条报错。

## 归属与转移

每条记录有且只有一个**负责人（Owner）**。它就是表单上的那个字段、"我的……"视图所过滤的那个字段，也是上面每一条规则说"记录的负责人"时所指的那个字段——一列，一个答案。改动负责人是真正的交接：原负责人立即失去基于归属的访问权，新负责人立即获得，且在所有位置同步生效。

正因为它会移动访问权，**修改负责人是一项独立的权限**，与普通的编辑权限分开：

| 角色 | 可转移的对象 |
| --- | --- |
| 系统管理员 | 全部对象 |
| 销售经理 | 销售簿——线索、客户、联系人、商机、报价、任务、会议、预测 |
| 客服专员 | 仅任务（以便升级时把工作交给客户负责人） |
| 销售代表、市场用户 | 无——记录是被指派给他们的 |

有四类日常操作**不算**转移，因而不需要该权限：

- **新建记录。** 新记录会自动归属到创建者，你无需手动选择自己。
- **保存表单**时未改动负责人——即使表单会把该字段一并提交。
- **自动化代你执行**——分配新线索的轮询、夜间生成续约与跟进任务的批处理。
- **认领一张无主工单。** 把一张没有任何人负责的工单转入 *In Progress*、*Waiting on Customer* 或 *Waiting on Support*，你就成了它的负责人（见上文*一条为「被用掉」而设的授权*）。负责人确实变了——但这个动作没有指定任何人、没有把记录从任何人手里拿走，而且只在工单无主时才起作用，所以它不是一次转移，也不需要转移权限。

没有该权限的用户尝试转移时会收到明确的拒绝提示，而不是悄无声息地无效。需要整体移交业务簿时，请联系管理员或你的销售经理。

> **从旧版本升级？** 负责人过去是一个普通字段，任何有编辑权的人都能改——而改动它只会让记录在各种列表和报表里移动，访问权**完全没动**。现在两者是同一个字段。此前显示负责人与实际访问归属已经产生偏离的记录，需要一次性回填对齐；详见发行说明中的回填说明。

### 私有商机

商机上的 `Private` 在上述一切之上生效：用户标记为 **Private** 的交易只对其所有人（以及管理员）可见，即使是像 Sales Manager 和 Marketing User 这类本来持有组织范围商机读取权限的权限档案也看不到。用于董事会级或涉并购敏感的交易。

## 字段级安全 (FLS)

FLS 控制在一条 *可见* 记录内部，某个权限档案能看到和编辑 **哪些字段** —— 例如客户 **健康度评分** 对所有能看到该客户的人可读，但只有销售经理能设置它；而 **工单内部备注** 对销售代表完全隐藏。随产品提供的遮罩清单见[权限档案](/zh-Hans/docs/administration/profiles#字段级安全)。

FLS 在 **所有地方** 生效 —— 列表视图、报表、API 和 AI Copilot。被遮罩的字段也不能用于过滤、排序或分组。

## AI Copilot 的共享

Copilot 代表用户行动 —— 它看到的与用户看到的完全一致，不多一分：

- 对象权限、共享和 FLS 全部自动生效。
- 管理员运行 Copilot 会比代表看到更多 —— 这是设计使然。

不存在“AI 绕过”。

## 给管理员的提示

- ✅ **先检查对象授权。** 空白的相关列表或报错的导航项，几乎总是缺少对象权限，而不是共享问题。
- ✅ **从严开始** —— 线索/客户/商机用私有 OWD，然后用共享规则放开。
- ✅ **记住岗位是扁平的。** 授予经理并不等于授予总监；两者都要写。
- ✅ **每季度审计共享规则** —— 它们会累积。
- ✅ 不要滥用 **查看所有** —— 它是一把绕过所有共享规则的大锤。

## 给用户的提示

如果你认为自己应该能看到某条记录却看不到：

1. 查一下 **谁拥有它**。
2. 如果你是从客户点进去的，先看 **它是哪个对象**：即使客户本身已经共享给你，报价、合同、任务、工单和交易仍归各自的所有人。联系人是唯一从不会被扣下的相关列表 —— Contact 是“由父级控制”，它跟随客户，所以客户共享给你时联系人也一并到达（见 *“由父级控制”在实践中*）。
3. 查一下该交易是否被标记为 **Private**。
4. 询问管理员是否应该有一条 **共享规则** 覆盖你的岗位。
